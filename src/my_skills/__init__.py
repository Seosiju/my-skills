"""my-skills — personal cross-agent Agent Skill registry.

Phase 1 (portable skill core): manifest loading, frontmatter parsing,
Agent Skills validation, a static security scan, a host registry, and the
``validate`` / ``doctor`` CLI commands.
"""

__version__ = "0.2.0"
